import{_ as s,o as e,c as a,X as t}from"./chunks/framework.92e7fb7b.js";const g=JSON.parse('{"title":"mysql","description":"","frontmatter":{},"headers":[],"relativePath":"blogs/coding-learning-record/mysql.md","filePath":"blogs/coding-learning-record/mysql.md"}'),l={name:"blogs/coding-learning-record/mysql.md"},n=t(`<h1 id="mysql" tabindex="-1">mysql <a class="header-anchor" href="#mysql" aria-label="Permalink to &quot;mysql&quot;">​</a></h1><h2 id="默认引擎" tabindex="-1">默认引擎 <a class="header-anchor" href="#默认引擎" aria-label="Permalink to &quot;默认引擎&quot;">​</a></h2><ul><li>InnoDB</li><li>MyISAM</li></ul><p>InnoDB 和 MyISAM 是 MySQL 数据库中的两个存储引擎，它们在功能和性能上有一些区别。让我详细解释一下：</p><p>事务支持： InnoDB 支持事务，而 MyISAM 不支持。对于 InnoDB，每一条 SQL 语句都默认封装成事务进行提交，这可能会影响速度。优化速度的方式是将多条 SQL 语句放在 BEGIN 和 COMMIT 之间，组成一个事务。 如果你的应用需要高度的事务处理，选择 InnoDB 是更好的选择。 外键支持： InnoDB 支持外键，而 MyISAM 不支持。外键是关系数据库中的重要概念，用于维护表之间的数据完整性。 如果你的数据模型需要外键约束，选择 InnoDB。 索引结构： MyISAM 使用 B+ 树的非聚集索引，存储的是实际数据的地址值，索引和实际数据是分开的。 InnoDB 使用 B+ 树的聚集索引，存储的是实际的数据。因此，InnoDB 表本身就是按照主键聚集的。 查询性能： MyISAM 在读取方面的性能较好，但在写入和并发性能方面相对较弱。 InnoDB 在写入和并发性能方面更强，但读取速度稍慢。 表行数统计： MyISAM 保存有表的总行数，可以直接使用 SELECT COUNT(<em>) FROM table; 获取。 InnoDB 没有保存表的总行数，因此在使用 SELECT COUNT(</em>) FROM table; 时会遍历整个表，消耗较大。但在加了 WHERE 条件后，MyISAM 和 InnoDB 处理方式相同。 总之，如果你的应用需要事务支持、外键约束以及更好的并发性能，选择 InnoDB。如果你对读取速度和全文索引有更高的要求，或者处理静态数据，那么 MyISAM 可能更适合你。12345</p><p>执行sql报错 <a href="http://www.kurui.me/mysql-error-1364.html" target="_blank" rel="noreferrer">http://www.kurui.me/mysql-error-1364.html</a></p><h2 id="关闭mysql的strict-mode" tabindex="-1">关闭mysql的strict mode <a class="header-anchor" href="#关闭mysql的strict-mode" aria-label="Permalink to &quot;关闭mysql的strict mode&quot;">​</a></h2><p>注释掉 my.ini中的</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;"># Set the SQL mode to strict</span></span>
<span class="line"><span style="color:#A6ACCD;"># sql-mode=&quot;STRICT_TRANS_TABLES,NO_AUTO_CREATE_USER,NO_ENGINE_SUBSTITUTION&quot;</span></span></code></pre></div><p>推荐sql学习</p><p><a href="https://github.com/liyupi/sql-mother" target="_blank" rel="noreferrer">https://github.com/liyupi/sql-mother</a></p><h2 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to &quot;总结&quot;">​</a></h2><ul><li><p>join inner join 交集 left join right join 左右其中一张的全表 加上选集 cross join 笛卡尔积</p></li><li><p>聚合 HAVING 筛选goup by的子句</p></li><li><p>创建新的聚合字段 sum rank 开窗函数 中分组 PARTITION BY</p></li><li><p>union union all 联表</p></li></ul><h3 id="做题内容" tabindex="-1">做题内容 <a class="header-anchor" href="#做题内容" aria-label="Permalink to &quot;做题内容&quot;">​</a></h3><p>select name,score from student where name not like &#39;%李%&#39;</p><p>-- 请在此处输入 SQL select name,score from student where name like &#39;%李%&#39; or score&gt;&#39;500&#39;</p><p>请编写一条 SQL 查询语句，从名为 student 的数据表中选择出所有不重复的班级 ID（class_id）和考试编号（exam_num）的组合。</p><p>select distinct class_id,exam_num from student</p><p>请编写一条 SQL 查询语句，从名为 student 的数据表中选择出学生姓名（name）、年龄（age）和成绩（score），首先按照成绩从大到小排序，如果成绩相同，则按照年龄从小到大排序。</p><p>-- 请在此处输入 SQL select name,age,score from student order by score desc,age asc;</p><p>请编写一条 SQL 查询语句，从名为 student 的数据表中选择学生姓名（name）和年龄（age），按照年龄从小到大排序，从第 2 条数据开始、截取 3 个学生的信息。</p><p>-- 请在此处输入 SQL select name,age from student order by age asc limit 1,3</p><p>假设有一个学生表 student，包含以下字段：name（姓名）、age（年龄）。请你编写一个 SQL 查询，将学生按照年龄划分为三个年龄等级（age_level）：60 岁以上为 &quot;老同学&quot;，20 岁以上（不包括 60 岁以上）为 &quot;年轻&quot;，20 岁及以下、以及没有年龄信息为 &quot;小同学&quot;。</p><p>返回结果应包含学生的姓名（name）和年龄等级（age_level），并按姓名升序排序。</p><p>SELECT name, CASE WHEN (age &gt; 60) THEN &#39;老同学&#39; WHEN (age &gt; 20) THEN &#39;年轻&#39; ELSE &#39;小同学&#39; END AS age_level FROM student ORDER BY name asc;</p><p>select name, date() as 当前日期 from student</p><p>假设有一个学生表 student，包含以下字段：id（学号）、name（姓名）。请你编写一个 SQL 查询，筛选出姓名为 &#39;热dog&#39; 的学生，展示其学号（id）、姓名（name）及其大写姓名（upper_name）。</p><p>-- 请在此处输入 SQL select id as &#39;id&#39;,name as &#39;name&#39;,UPPER(name) AS &#39;upper_name&#39; from student where name=&#39;热dog&#39;</p><p>假设有一个学生表 student，包含以下字段：id（学号）、name（姓名）、class_id（班级编号）、score（成绩）。请你编写一个 SQL 查询，汇总学生表中所有学生的总成绩（total_score）、平均成绩（avg_score）、最高成绩（max_score）和最低成绩（min_score）。</p><p>select sum(score) as total_score, AVG(score) as avg_score, max(score) as max_score, min(score) as min_score from student</p><p>假设有一个学生表 student，包含以下字段：id（学号）、name（姓名）、class_id（班级编号）、score（成绩）。请你编写一个 SQL 查询，统计学生表中的班级编号（class_id）和每个班级的平均成绩（avg_score）。</p><p>-- 请在此处输入 SQL select class_id, avg(score) as avg_score from student group by class_id</p><p>假设有一个学生表 student，包含以下字段：id（学号）、name（姓名）、class_id（班级编号）、exam_num（考试次数）、score（成绩）。请你编写一个 SQL 查询，统计学生表中每个班级每次考试的总学生人数（total_num）。</p><p>-- 请在此处输入 SQL select class_id, exam_num, COUNT(*) as total_num from student group by class_id,exam_num</p><p>假设有一个学生表 student，包含以下字段：id（学号）、name（姓名）、class_id（班级编号）、score（成绩）。请你编写一个 SQL 查询，统计学生表中班级的总成绩超过 150 分的班级编号（class_id）和总成绩（total_score）。</p><p>-- 请在此处输入 SQL select * from (select class_id, sum(score) as total_score from student group by class_id) where total_score&gt;150</p><ul><li>HAVING子句</li></ul><p>对goup by的内容进行筛选</p><p>2）使用 HAVING 子句查询订单总金额超过 100 的客户： -- 查询总成绩超过200的班级 SELECT customer_id, SUM(amount) AS total_amount FROM orders GROUP BY customer_id HAVING SUM(amount) &gt; 100;</p><p>假设有一个学生表 student，包含以下字段：id（学号）、name（姓名）、class_id（班级编号）、score（成绩）。请你编写一个 SQL 查询，统计学生表中班级的总成绩超过 150 分的班级编号（class_id）和总成绩（total_score）。</p><p>SELECT class_id, SUM(score) AS total_score FROM student GROUP BY class_id HAVING SUM(score) &gt; 150;</p><ul><li>cross join</li></ul><p>其中，CROSS JOIN 是一种简单的关联查询，不需要任何条件来匹配行，它直接将左表的 每一行 与右表的 每一行 进行组合，返回的结果是两个表的笛卡尔积。</p><p>假设有一个学生表 student ，包含以下字段：id（学号）、name（姓名）、age（年龄）、class_id（班级编号）；还有一个班级表 class ，包含以下字段：id（班级编号）、name（班级名称）。</p><p>请你编写一个 SQL 查询，将学生表和班级表的所有行组合在一起，并返回学生姓名（student_name）、学生年龄（student_age）、班级编号（class_id）以及班级名称（class_name）。</p><p>select s.name as student_name, s.age as student_age, s.class_id as class_id, c.name as class_name from student s cross join class c</p><ul><li>INNER JOIN 条件连接 数量是两表的交际</li></ul><p>-- 请在此处输入 SQL select s.name as student_name, s.age as student_age, s.class_id as class_id, c.name as class_name, c.level as class_level FROM student s JOIN class c ON s.class_id = c.id</p><ul><li>outer join</li></ul><p>LEFT OUTER JOIN 和 RIGHT OUTER JOIN</p><p>在 OUTER JOIN 中，包括 LEFT OUTER JOIN 和 RIGHT OUTER JOIN 两种类型，它们分别表示查询左表和右表的所有行（即使没有被匹配），再加上满足条件的交集部分。 左表或者右表长度 加上交集</p><p>实现right join 主表 关联表顺序需要调换</p><p>-- 请在此处输入 SQL select s.name as student_name, s.age as student_age, s.class_id as class_id, c.name as class_name, c.level as class_level from student s left join class c on s.class_id = c.id</p><p>假设有一个学生表 student，包含以下字段：id（学号）、name（姓名）、age（年龄）、score（分数）、class_id（班级编号）。还有一个班级表 class，包含以下字段：id（班级编号）、name（班级名称）。</p><p>请你编写一个 SQL 查询，使用子查询的方式来获取存在对应班级的学生的所有数据，返回学生姓名（name）、分数（score）、班级编号（class_id）字段。</p><p>-- 请在此处输入 SQL -- select s.name,s.score,c.id as class_id -- from student s -- cross join class c where c.id = s.id select name,score,class_id from student where class_id IN(SELECT DISTINCT id from class)</p><p>假设有一个学生表 student，包含以下字段：id（学号）、name（姓名）、age（年龄）、score（分数）、class_id（班级编号）。还有一个班级表 class，包含以下字段：id（班级编号）、name（班级名称）。</p><p>请你编写一个 SQL 查询，使用 exists 子查询的方式来获取 不存在对应班级的 学生的所有数据，返回学生姓名（name）、年龄（age）、班级编号（class_id）字段。</p><p>-- 请在此处输入 SQL select name,age,class_id from student where not exists(select 1 FROM class where student.class_id = class.id)</p><ul><li>union all</li></ul><p>包括两种常见的组合查询操作：UNION 和 UNION ALL。</p><p>UNION 操作：它用于将两个或多个查询的结果集合并， 并去除重复的行 。即如果两个查询的结果有相同的行，则只保留一行。</p><p>UNION ALL 操作：它也用于将两个或多个查询的结果集合并， 但不去除重复的行 。即如果两个查询的结果有相同的行，则全部保留。</p><p>假设有一个学生表 student，包含以下字段：id（学号）、name（姓名）、age（年龄）、score（分数）、class_id（班级编号）。还有一个新学生表 student_new，包含的字段和学生表完全一致。</p><p>请编写一条 SQL 语句，获取所有学生表和新学生表的学生姓名（name）、年龄（age）、分数（score）、班级编号（class_id）字段，要求保留重复的学生记录。</p><p>-- 请在此处输入 SQL select name,age,score,class_id from student union all select name,age,score,class_id from student_new</p><ul><li>开窗函数：sum over</li></ul><p>打个比方，可以将开窗函数想象成一种 &quot;透视镜&quot;，它能够将我们聚焦在某个特定的分组，同时还能看到整体的全景。</p><p>本节我们先讲第一个开窗函数：sum over。</p><p>该函数用法为：</p><p>假设有一个学生表 student，包含以下字段：id（学号）、name（姓名）、age（年龄）、score（分数）、class_id（班级编号）。</p><p>请你编写一个 SQL 查询，返回每个学生的详细信息（字段顺序和原始表的字段顺序一致），并计算每个班级的学生平均分（class_avg_score）。</p><p>SELECT id, name, age, score, class_id, AVG(score) OVER (PARTITION BY class_id) AS class_avg_score FROM student;</p><ul><li>sum over order by 组内数据累加 依次求和</li></ul><p>假设有一个学生表 student，包含以下字段：id（学号）、name（姓名）、age（年龄）、score（分数）、class_id（班级编号）。</p><p>请你编写一个 SQL 查询，返回每个学生的详细信息（字段顺序和原始表的字段顺序一致），并且按照分数升序的方式累加计算每个班级的学生总分（class_sum_score）。</p><p>select id, name, age, score, class_id,sum(score) over (partition by class_id order by score asc) as class_sum_score from student</p><ul><li>partition by 相当于groupby</li></ul><p>根据xxx分组 此处为根据班级分组 假设有一个学生表 student，包含以下字段：id（学号）、name（姓名）、age（年龄）、score（分数）、class_id（班级编号）。</p><p>请你编写一个 SQL 查询，返回每个学生的详细信息（字段顺序和原始表的字段顺序一致），并且按照分数降序的方式计算每个班级内的学生的分数排名（ranking）。</p><p>select id,name,age,score,class_id, Rank() OVER ( PARTITION BY class_id //根据班级分组 ORDER BY score desc) as ranking from student</p><ul><li>Row_Number 几乎和rank一样 -- 请在此处输入 SQL select id,name,age,score,class_id, ROW_NUMBER() OVER( PARTITION BY class_id ORDER BY score desc) as row_number from student</li></ul><p>完成</p><h2 id="总结难点" tabindex="-1">总结难点 <a class="header-anchor" href="#总结难点" aria-label="Permalink to &quot;总结难点&quot;">​</a></h2><p>join<br> inner join 交集<br> left join right join 左右其中一张的全表 加上选集<br> cross join 笛卡尔积</p><p>聚合<br> HAVING 筛选goup by的子句</p><p>创建新的聚合字段<br> sum<br> rank 开窗函数 中分组 PARTITION BY</p><p>union union all 联表</p>`,88),o=[n];function c(r,i,d,p,m,u){return e(),a("div",null,o)}const S=s(l,[["render",c]]);export{g as __pageData,S as default};
